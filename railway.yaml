project: roamly
services:
  api:
    build:
      context: .
    deploy:
      numReplicas: 1
      startCommand: cd roamly-backend && pip install -r requirements.txt && python main.py
      healthcheckPath: /api/itinerary?zip_code=10001